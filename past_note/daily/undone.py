df
.pct_change()
.corr()
.cov()

5.3
-------------

.map() map函数能把一个函数用于一个序列上
.applymap()
value = value.strip()
value = re.sub('[!#?]', '', value)
value = value.title()

python 错误异常处理

7.3正则表达式

DataFrame.drop_duplicates(subset=None, keep='first', inplace=False)

示例：
df.drop_duplicats(subset = ['price','cnt'],keep='last',inplace=True)


8.2 Merging on Index
np.where(pd.isnull(s1), s2, s1)

--------------------------------
